# دليل العمل على موقع النادي الهندسي

> هذا الملف لأي شخص أو أداة ذكاء اصطناعي تشتغل على المشروع (Claude Code، Antigravity، Cursor…).
> اقرأه كاملاً قبل أي تعديل. القواعد هنا ليست تفضيلات شكلية: كسرها يعطّل الموقع أو يمسح بيانات طلبة حقيقيين.

**English summary for tools:** React 19 + TypeScript + Vite + Tailwind 4 site with a Supabase backend.
Never run database DDL from code; every schema change is a numbered SQL file the owner runs by hand.
The UI language is Arabic (RTL). Keep comments in English, keep user-facing text in Arabic.

---

## 1. شو هو المشروع

موقع النادي الهندسي في جامعة فلسطين: صفحة عامة للطلبة + لوحة إدارة + حساب عضو.

| | |
|---|---|
| الموقع | https://engineering-club-phi.vercel.app |
| المستودع | github.com/mohammedalnwerie/engineering-club (فرع `main`) |
| النشر | Vercel، تلقائي عند كل `push` إلى `main` |
| قاعدة البيانات | Supabase (مشروع `zcfixuqhozssbkllybru`) |
| لوحة الإدارة | `/#/admin` — دخول بحساب Supabase Auth |
| حساب العضو | `/#/member` — الرقم الجامعي + كلمة المرور |

---

## 2. القواعد الحمراء (ممنوع كسرها)

1. **ممنوع تشغيل أوامر تعديل قاعدة البيانات من الكود.** لا `create table`، لا `alter`، ولا سكربتات ترحيل.
   كل تغيير على قاعدة البيانات يُكتب في ملف `supabase/update-0NN-<وصف>.sql` ويشغّله المالك بيده في SQL Editor.
2. **مفتاح الخدمة (service_role) لا يدخل الموقع أبداً.** الواجهة تستخدم مفتاح `anon` فقط.
   أي عملية تحتاج صلاحية أعلى تذهب إلى Edge Function.
3. **ممنوع إرسال بيانات حقيقية للتجربة** (طلبات انضمام، شكاوى، إيميلات) على الموقع المنشور.
   للتجربة: بيانات خاطئة عمداً ترفضها قاعدة البيانات، أو نسخة محلية.
4. **رمز العضو (`member_code`) لا يخرج للعامة.** دالة `verify_member` ترجع آخر 4 خانات فقط (`codeHint`).
5. **كلمات المرور لا تُطبع ولا تُخزن كنص.** كلمة مرور العضو تُخزَّن مشفّرة (bcrypt) داخل قاعدة البيانات.
6. **لا تحذف بيانات الطلبة.** الحذف يمرّ على `club_trash` (سلة 30 يوماً) عبر التريغرات الموجودة.
7. **كل نص يراه المستخدم بالعربي**، والواجهة RTL. التعليقات داخل الكود بالإنجليزية.

---

## 3. سير العمل

```bash
npm install
npm run dev      # خادم محلي
npm run build    # لازم ينجح قبل أي commit
npm run lint
```

- الفرع الوحيد `main`. لا تعمل push بدون بناء ناجح.
- رسالة الـcommit: سطر أول يشرح *ماذا تغيّر ولماذا*، ثم فقرة قصيرة عند الحاجة.
- كل commit من أداة ذكاء اصطناعي ينتهي بسطر `Co-Authored-By:` المناسب لها.

---

## 4. خريطة الكود

```
src/
  App.tsx                     التوجيه (hash routes) وتركيب الصفحة العامة
  components/                 أقسام الموقع العام + النوافذ (Modals)
    admin/                    أقسام لوحة الإدارة الجديدة + مكوّنات مشتركة
      ui.tsx                  الأزرار، اللوحات، الحقول، التواريخ  ← استخدمها دائماً
      controls.tsx            قائمة الإجراءات، نافذة التأكيد، مربع الاختيار
      adminApi.ts             كل قراءة/كتابة للوحة الإدارة عبر supabase-js
    member/                   صفحة «حسابي»
  services/
    supabaseClient.ts         publicSelect / publicRpc (fetch خفيف) + getSupabase() الكسول
    dataService.ts            محتوى الموقع + الطلبات + الشكاوى (مع كاش وذاكرة محلية)
    memberService.ts          جلسة العضو واستدعاءات دواله
    emailService.ts           نصوص الإيميل/واتساب + استدعاء دالة إيميل القبول
  utils/                      التحقق من المدخلات، رسم البطاقة (canvas)، الأمان
  data/                       اللجان، التصنيفات، الروابط الرسمية، المحتوى الافتراضي
supabase/
  schema.sql                  المرجع الكامل (كل التحديثات مدموجة فيه بالترتيب)
  update-0NN-*.sql            ملفات التحديث المرقّمة — تُشغَّل يدوياً بالترتيب
  functions/<name>/index.ts   Edge Functions (تُنشر يدوياً من لوحة Supabase)
```

### مبدأ مهم
- **الموقع العام** يستخدم `publicSelect` / `publicRpc` فقط (بدون تحميل مكتبة supabase-js) حتى يبقى خفيفاً.
- **لوحة الإدارة** فقط هي التي تحمّل supabase-js عبر `getSupabase()` (تحميل كسول).
- الصلاحيات تُفرض في قاعدة البيانات (RLS + دوال security definer)، وليس في الواجهة. الواجهة تخفي الأزرار فقط.

---

## 5. الأدوار والصلاحيات

| الدور | ماذا يرى |
|---|---|
| `owner` | كل شيء + منح صلاحية المالك |
| `vp_admin` | كل شيء |
| `tech_support` | كل شيء |
| `media` | طلبات وفعاليات «اللجنة الإعلامية» فقط |

مصدر الحقيقة: عمود `role` في `club_admins` + دوال `club_admin_role()` و`club_has_full_access()`.
أي قسم جديد في اللوحة يجب أن يحترم `fullAccess` في الواجهة **و** سياسة RLS في قاعدة البيانات.

---

## 6. نموذج العضوية

- عند قبول الطلب: يصدر `member_code` تلقائياً + بطاقة أولى صالحة (14 يوماً أو حتى تاريخ ثابت تحدده الإدارة).
- بعدها: عضوية فصلية برسوم (المبلغ والفصل من إعدادات `club_content.membership`).
- العضو يدخل أول مرة برمز البطاقة ثم **يعيّن كلمة مرور**؛ بعدها يتوقف الرمز عن العمل للدخول.
- الإدارة تستطيع **تعليق** العضوية (`suspended_at`) بدون حذف العضو.
- التسجيل في الفعاليات للأعضاء ذوي العضوية السارية فقط (تتحقق منه قاعدة البيانات، لا الواجهة).

---

## 7. قبل ما تسلّم أي تعديل

- [ ] `npm run build` ينجح.
- [ ] النصوص الجديدة بالعربي وواضحة لطالب عادي (بدون مصطلحات تقنية).
- [ ] الأزرار التي تُنفّذ إجراءً لها نص مكتوب، وليست أيقونة صامتة.
- [ ] مساحة اللمس 44×44 بكسل على الأقل، وحجم الخط 12 بكسل فأكثر.
- [ ] يعمل على عرض 375 بكسل (موبايل) بدون تمرير أفقي.
- [ ] الحذف يمر على السلة، والإجراءات الخطرة لها نافذة تأكيد (`useConfirm`).
- [ ] أي تغيير في قاعدة البيانات = ملف `update-0NN` جديد + دمجه في `schema.sql` + إخبار المالك بخطوات تشغيله.
